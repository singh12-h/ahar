// Compile-safe stub for dart:js when compiled on native platforms (Android/iOS/Desktop)

class JsStub {
  const JsStub();

  dynamic callMethod(String method, [List? args]) {
    return null;
  }

  operator [](dynamic key) => null;
}

const context = JsStub();
